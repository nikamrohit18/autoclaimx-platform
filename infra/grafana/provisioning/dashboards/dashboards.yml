apiVersion: 1

providers:
  - name: AutoClaimX
    orgId: 1
    folder: AutoClaimX
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
